let defaultB = {
    height: '100vh',
    background: 'rgb(31, 37, 43)'    
};
let dynamic = {
    height: '100vh'
};
let blurred = {
    height: '100vh',
    backgroundPosition: 'center',
    backgroundSize: 'cover',
    filter: 'blur(20px)'
};

export { defaultB, dynamic, blurred }